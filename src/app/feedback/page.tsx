import { TopBar } from '@/components/AppShell'; import { FeedbackCard } from '@/components/Marketplace'; import { feedback } from '@/lib/mock-data';
export default function Feedback(){return <><TopBar title="Feedback" subtitle="Review a required 20-second section to earn your next submission."/><div className="mb-6 grid gap-3 sm:grid-cols-3">{[['Eligibility','Review one queued song to submit again.'],['Queue health','42 songs waiting · median response 18 min.'],['Submission status','One active submission per user.']].map(([title,copy])=><div key={title} className="surface rounded-xl p-4"><p className="eyebrow">{title}</p><p className="mt-2 text-sm leading-5 text-zinc-300">{copy}</p></div>)}</div><div className="space-y-4">{feedback.map(f=><FeedbackCard key={f.id} item={f}/>)}</div></>}
