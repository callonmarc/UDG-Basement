import { TopBar } from '@/components/AppShell'; import { PostCard, PostComposer, UserCard } from '@/components/Social'; import { posts, users } from '@/lib/mock-data';
export default function Home(){return <><TopBar title="Home" subtitle="A live feed for artists, producers, writers, DJs, and engineers."/><div className="mb-4 flex rounded-2xl bg-white/5 p-1"><button className="flex-1 rounded-xl bg-white/10 py-3 font-bold">For You</button><button className="flex-1 py-3 font-bold text-zinc-400">Following</button></div><div className="space-y-4"><PostComposer/>{posts.map(p=><PostCard key={p.id} post={p}/>)}</div><div className="mt-6 grid gap-4 sm:grid-cols-2">{users.slice(1,3).map(u=><UserCard key={u.id} user={u}/>)}</div></>}
