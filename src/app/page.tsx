import { TopBar } from '@/components/AppShell'; import { PostCard, PostComposer, UserCard } from '@/components/Social'; import { posts, users } from '@/lib/mock-data';
export default function Home(){return <><TopBar title="Home" subtitle="A live feed for artists, producers, writers, DJs, and engineers."/><div className="mb-5 flex border-b border-[#2b2d33]"><button className="border-b-2 border-neon px-5 py-3 text-sm font-bold text-neon">For You</button><button className="border-b-2 border-transparent px-5 py-3 text-sm font-semibold text-zinc-500">Following</button></div><div className="space-y-4"><PostComposer/>{posts.map(p=><PostCard key={p.id} post={p}/>)}</div><section className="mt-8"><div className="mb-3 flex items-baseline justify-between"><h2 className="text-xl font-black">Creators to know</h2><button className="text-sm font-semibold text-neon">See all</button></div><div className="grid gap-4 sm:grid-cols-2">{users.slice(1,3).map(u=><UserCard key={u.id} user={u}/>)}</div></section></>}
