import { TopBar } from '@/components/AppShell'; import { MusicPlayer } from '@/components/MusicPlayer'; import { UserCard } from '@/components/Social'; import { songs, users } from '@/lib/mock-data';
export default function Discover(){return <><TopBar title="Discover" subtitle="Search-ready surfaces for artists, songs, beats, posts, and feature listings."/><section className="surface mb-6 rounded-2xl p-4"><input aria-label="Search the UDG Basement community" className="w-full rounded-xl border border-[#30323a] bg-[#0c0d10] px-4 py-3 text-sm text-zinc-100 outline-none placeholder:text-zinc-600" placeholder="Search by genre, artist, producer, location, price..."/><div className="mt-3 flex flex-wrap gap-2">{['R&B','Hip-Hop','Pop','Producer','Atlanta','Free','Paid'].map((f,i)=><button className={`rounded-md px-3 py-1.5 text-xs font-semibold ${i === 0 ? 'bg-neon text-black' : 'bg-[#202126] text-zinc-400'}`} key={f}>{f}</button>)}</div></section><h2 className="mb-3 text-xl font-black">People in the room</h2><div className="grid gap-4 lg:grid-cols-2">{users.map(u=><UserCard key={u.id} user={u}/>)}</div><h2 className="mb-3 mt-8 text-xl font-black">Songs bubbling up</h2><div className="grid gap-4 lg:grid-cols-2">{songs.map(s=><MusicPlayer key={s.id} song={s}/>)}</div></>}
