import { TopBar } from '@/components/AppShell'; import { MusicPlayer } from '@/components/MusicPlayer'; import { UserCard } from '@/components/Social'; import { songs, users } from '@/lib/mock-data';
export default function Discover(){return <><TopBar title="Discover" subtitle="Search-ready surfaces for artists, songs, beats, posts, and feature listings."/><div className="glass mb-5 rounded-3xl p-4"><input className="w-full rounded-2xl border border-white/10 bg-black/20 px-4 py-3 outline-none" placeholder="Search by genre, artist, producer, location, price..."/><div className="mt-3 flex flex-wrap gap-2">{['R&B','Hip-Hop','Pop','Producer','Atlanta','Free','Paid'].map(f=><span className="rounded-full bg-white/10 px-3 py-1 text-sm" key={f}>{f}</span>)}</div></div><div className="grid gap-4 lg:grid-cols-2">{users.map(u=><UserCard key={u.id} user={u}/>)}</div><h2 className="mb-3 mt-7 text-2xl font-black">Songs bubbling up</h2><div className="grid gap-4 lg:grid-cols-2">{songs.map(s=><MusicPlayer key={s.id} song={s}/>)}</div></>}
