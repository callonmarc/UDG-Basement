import { User } from '@/lib/types'; import { Avatar } from './Social';
export function ProfileHeader({user}:{user:User}){return <div className="overflow-hidden rounded-3xl border border-white/10 bg-white/5"><img src={user.banner} alt="" className="h-44 w-full object-cover"/><div className="p-5"><div className="-mt-14 flex items-end gap-4"><img src={user.avatar} alt="" className="h-28 w-28 rounded-3xl object-cover ring-4 ring-ink"/><button className="ml-auto rounded-full bg-neon px-5 py-2 font-black text-black">Follow</button></div><h1 className="mt-4 text-3xl font-black">{user.displayName}</h1><p className="text-zinc-400">@{user.username} · {user.role} · {user.location}</p><p className="mt-3 max-w-2xl text-zinc-200">{user.bio}</p><div className="mt-4 flex gap-5 text-sm"><b>{user.followers.toLocaleString()} <span className="text-zinc-500">followers</span></b><b>{user.following.toLocaleString()} <span className="text-zinc-500">following</span></b></div><div className="mt-4 flex flex-wrap gap-2">{['Posts','Music','Features','Beats','Feedback'].map(t=><span key={t} className="rounded-full bg-white/10 px-4 py-2 text-sm font-bold">{t}</span>)}</div></div></div>}
