import { User } from '@/lib/types';
export function ProfileHeader({user}:{user:User}) { return <section className="surface overflow-hidden rounded-2xl"><img src={user.banner} alt="" className="h-32 w-full object-cover sm:h-44"/><div className="p-4 sm:p-5"><div className="-mt-12 flex items-end gap-3 sm:-mt-14"><img src={user.avatar} alt={user.displayName} className="h-24 w-24 rounded-2xl object-cover ring-4 ring-[#111216] sm:h-28 sm:w-28"/><button className="accent-button ml-auto min-h-10 rounded-lg px-4 text-sm font-black">Follow</button></div><h1 className="mt-4 text-2xl font-black tracking-tight sm:text-3xl">{user.displayName}</h1><p className="mt-1 text-sm text-zinc-500">@{user.username} · {user.role} · {user.location}</p><p className="mt-3 max-w-2xl text-sm leading-6 text-zinc-300">{user.bio}</p><div className="mt-4 flex flex-wrap gap-x-5 gap-y-1 text-sm"><b>{user.followers.toLocaleString()} <span className="font-normal text-zinc-500">followers</span></b><b>{user.following.toLocaleString()} <span className="font-normal text-zinc-500">following</span></b></div><div className="mt-5 flex overflow-x-auto border-b border-[#2b2d33] text-sm font-semibold text-zinc-500">{['Posts','Music','Features','Beats','Feedback'].map((t, i) => <button key={t} className={`shrink-0 border-b-2 px-3 py-3 ${i === 0 ? 'border-neon text-neon' : 'border-transparent'}`}>{t}</button>)}</div></div></section>; }
