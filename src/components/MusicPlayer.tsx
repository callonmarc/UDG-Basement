import { Pause, Play } from 'lucide-react'; import type { Song } from '@/lib/types';
export function MusicPlayer({song,compact=false}:{song:Song;compact?:boolean}){return <div className="rounded-2xl border border-white/10 bg-black/25 p-3"><div className="flex items-center gap-3"><img src={song.coverArt} alt="" className="h-14 w-14 rounded-xl object-cover"/><button className="grid h-10 w-10 place-items-center rounded-full bg-neon text-black">{compact?<Play size={18}/>:<Pause size={18}/>}</button><div className="min-w-0 flex-1"><p className="truncate font-bold">{song.title}</p><p className="truncate text-sm text-zinc-400">{song.artist} · {song.genre}</p><div className="mt-2 h-1.5 rounded-full bg-white/10"><div className="h-full w-2/5 rounded-full bg-neon"/></div></div><span className="text-xs text-zinc-400">{song.duration}</span></div></div>}
